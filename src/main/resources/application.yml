spring:
  datasource:
    url: jdbc:mysql://dbserver:3306/kotlin
     # mysql username & password
#    driver-class-name: net.sf.log4jdbc.sql.jdbcapi.DataSourceSpy
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    database: mysql
logging:
  level:
    jdbc:
      sqltiming: DEBUG
#
#---
#spring:
#  profiles: dev-docker
